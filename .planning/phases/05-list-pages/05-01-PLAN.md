---
phase: 05-list-pages
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/pages/articles.astro
  - src/pages/categories/[category].astro
  - src/pages/tags/[tag].astro
autonomous: true
requirements:
  - DRAFT-05
user_setup: []
must_haves:
  truths:
    - "首页不显示草稿文章"
    - "文章归档页不显示草稿文章"
    - "分类页不显示草稿文章"
    - "标签页不显示草稿文章"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "首页文章列表"
      uses: "getPublishedPosts"
    - path: "src/pages/articles.astro"
      provides: "文章归档页"
      uses: "getPublishedPosts"
    - path: "src/pages/categories/[category].astro"
      provides: "分类页文章列表"
      uses: "getPublishedPosts"
    - path: "src/pages/tags/[tag].astro"
      provides: "标签页文章列表"
      uses: "getPublishedPosts"
---

<objective>
在所有列表页面添加草稿过滤：index.astro、articles.astro、categories/[category].astro、tags/[tag].astro。

Purpose: 确保所有列表页面都不显示草稿文章，完成 DRAFT-05 要求。

Output: 完成 4 个列表页面的草稿过滤。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-list-pages/05-CONTEXT.md

# 现有代码结构

src/pages/index.astro:
```typescript
const allPosts = await getCollection('posts');
```

src/pages/articles.astro:
```typescript
const allPosts = await getCollection('posts');
```

src/pages/categories/[category].astro:
```typescript
const posts = await getCollection('posts');
```

src/pages/tags/[tag].astro:
```typescript
const posts = await getCollection('posts');
```

所有页面都需导入 getPublishedPosts:
```typescript
import { getPublishedPosts } from '../utils/posts';
```

Helper 函数已在 Phase 4 创建: src/utils/posts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.astro to filter drafts</name>
  <files>src/pages/index.astro</files>
  <action>
    修改 index.astro，使用 getPublishedPosts() 替代 getCollection('posts')。

    1. 添加导入:
    ```typescript
    import { getPublishedPosts } from '../utils/posts';
    ```

    2. 修改获取文章:
    ```typescript
    // 修改前
    const allPosts = await getCollection('posts');

    // 修改后
    const allPosts = await getPublishedPosts();
    ```
  </action>
  <verify>
    <automated>grep -n "getPublishedPosts" src/pages/index.astro</automated>
  </verify>
  <done>index.astro 使用 getPublishedPosts</done>
</task>

<task type="auto">
  <name>Task 2: Update articles.astro to filter drafts</name>
  <files>src/pages/articles.astro</files>
  <action>
    修改 articles.astro，使用 getPublishedPosts() 替代 getCollection('posts')。

    1. 添加导入:
    ```typescript
    import { getPublishedPosts } from '../utils/posts';
    ```

    2. 修改获取文章:
    ```typescript
    // 修改前
    const allPosts = await getCollection('posts');

    // 修改后
    const allPosts = await getPublishedPosts();
    ```
  </action>
  <verify>
    <automated>grep -n "getPublishedPosts" src/pages/articles.astro</automated>
  </verify>
  <done>articles.astro 使用 getPublishedPosts</done>
</task>

<task type="auto">
  <name>Task 3: Update categories/[category].astro to filter drafts</name>
  <files>src/pages/categories/[category].astro</files>
  <action>
    修改 categories/[category].astro，使用 getPublishedPosts() 替代 getCollection('posts')。

    1. 添加导入:
    ```typescript
    import { getPublishedPosts } from '../../utils/posts';
    ```

    2. 修改获取文章:
    ```typescript
    // 修改前
    const posts = await getCollection('posts');

    // 修改后
    const posts = await getPublishedPosts();
    ```
  </action>
  <verify>
    <automated>grep -n "getPublishedPosts" src/pages/categories/[category].astro</automated>
  </verify>
  <done>categories/[category].astro 使用 getPublishedPosts</done>
</task>

<task type="auto">
  <name>Task 4: Update tags/[tag].astro to filter drafts</name>
  <files>src/pages/tags/[tag].astro</files>
  <action>
    修改 tags/[tag].astro，使用 getPublishedPosts() 替代 getCollection('posts')。

    1. 添加导入:
    ```typescript
    import { getPublishedPosts } from '../../utils/posts';
    ```

    2. 修改获取文章:
    ```typescript
    // 修改前
    const posts = await getCollection('posts');

    // 修改后
    const posts = await getPublishedPosts();
    ```
  </action>
  <verify>
    <automated>grep -n "getPublishedPosts" src/pages/tags/[tag].astro</automated>
  </verify>
  <done>tags/[tag].astro 使用 getPublishedPosts</done>
</task>

</tasks>

<verification>
构建测试:
1. 运行 `npm run build` 检查生产构建是否成功
2. 验证所有列表页面都使用 getPublishedPosts

文件检查:
- src/pages/index.astro 导入 getPublishedPosts
- src/pages/articles.astro 导入 getPublishedPosts
- src/pages/categories/[category].astro 导入 getPublishedPosts
- src/pages/tags/[tag].astro 导入 getPublishedPosts
</verification>

<success_criteria>
1. 首页不显示草稿文章
2. 文章归档页不显示草稿文章
3. 分类页不显示草稿文章
4. 标签页不显示草稿文章

验证命令: `grep -r "getPublishedPosts" src/pages/`
</success_criteria>

<output>
After completion, create `.planning/phases/05-list-pages/05-01-SUMMARY.md`
</output>
